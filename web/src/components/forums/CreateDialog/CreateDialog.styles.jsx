import {makeStyles} from '@mui/material/styles';

export const useStyles = makeStyles(() => ({
  root: {
    width: '800px',
  },
  switchContainer: {
    display: 'flex',
    alignItems: 'center',
  },
}));

