import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  root: {
    width: '800px',
  },
  switchContainer: {
    display: 'flex',
    alignItems: 'center',
  },
}));

