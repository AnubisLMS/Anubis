import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles(() => ({
  root: {
    width: '800px',
  },
  switchContainer: {
    display: 'flex',
    alignItems: 'center',
  },
}));

